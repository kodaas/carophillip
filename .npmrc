# Include optional dependencies (fixes Rollup platform-specific binaries)
include=optional

# Disable funding messages for cleaner CI output
fund=false

# Disable audit for faster installs in CI
audit=false

# Use legacy peer deps to avoid resolution conflicts
legacy-peer-deps=true

# Prefer offline packages when available
prefer-offline=false
